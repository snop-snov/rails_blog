#filters
  = search_form_for @search, :html => { :class => 'form-horizontal' } do |f|
    = f.label :title
    = f.text_field :title_cont
    = f.label :text
    = f.text_field :text_cont

    = f.submit
